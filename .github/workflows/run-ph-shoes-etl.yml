name: Run ph_shoes_etl DAG

on:
  workflow_dispatch:
    inputs:
      aws_region:
        description: 'AWS Region'
        required: true
        default: 'ap-southeast-1'
      ec2_instance_name:
        description: 'EC2 instance Name tag'
        required: true
        default: 'airflow-ec2'
      dag_id:
        description: 'Airflow DAG ID'
        required: true
        default: 'ph_shoes_etl'

env:
  AWS_REGION:            ${{ github.event.inputs.aws_region }}
  EC2_INSTANCE_NAME:     ${{ github.event.inputs.ec2_instance_name }}
  DAG_ID:                ${{ github.event.inputs.dag_id }}

jobs:
  trigger-dag:
    name: Trigger ph_shoes_etl on Airflow EC2
    runs-on: ubuntu-latest
    environment: main

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Configure AWS credentials for SSM
        uses: aws-actions/configure-aws-credentials@v3
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region:            ${{ env.AWS_REGION }}

      - name: Trigger ph_shoes_etl via SSM by Instance ID
        run: |
          INSTANCE_ID="i-03df87f324920dff1"  # or use your get_id output
          echo "Sending SSM command to $INSTANCE_ID"

          aws ssm send-command \
            --instance-ids "$INSTANCE_ID" \
            --document-name "AWS-RunShellScript" \
            --comment "Trigger ph_shoes_etl DAG" \
            --parameters commands='[
              "docker exec airflow-scheduler unpause ph_shoes_etl",
              "docker exec airflow-scheduler trigger ph_shoes_etl --run-id gh-action-'"$(date +%s)"'"
            ]' \
            --region $AWS_REGION \
            --output json